This Repo is Maxime's Udacity project to filter ride share data.
It will allow reading CSV files from ridesharing and display filtered values to the user using a command interface.
It's going to be used as an example to for future projects.

